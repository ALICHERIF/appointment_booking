<%= @room.name %>
<%= @room.capacity %>
<%= form_for @room, method: :delete, data: { confirm: "Are you sure you want to delete this example?" } do |f| %>
  <%= f.submit "Delete", class: "btn btn-danger" %>
<% end %>
